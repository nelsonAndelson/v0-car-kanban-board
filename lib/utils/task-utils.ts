import { AlertCircle, Circle, ArrowUp } from "lucide-react";

// Define priority icons
export const priorityIcons = {
  low: Circle,
  medium: AlertCircle,
  high: ArrowUp,
};

// Define priority colors
export const priorityColors = {
  low: "text-green-500",
  medium: "text-yellow-500",
  high: "text-red-500",
};
